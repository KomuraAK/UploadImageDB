Create a new database in your host!

Follow the command line:

create database DrUploadImages character set utf8mb4 collate utf8mb4_unicode_ci;



Use:

open project front-end:

npm start react

start back-end service:

node or nodemon app.js
